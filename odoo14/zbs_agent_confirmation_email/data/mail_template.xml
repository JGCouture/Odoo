<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="email_template_agent_sale_order_confirmation" model="mail.template">
            <field name="name">Agent sale order confirmation email</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and (ctx.get('proforma') and 'Proforma' or 'Quotation') or 'Order'} (Ref ${object.name or 'n/a' }) Confirmed.</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="email_to">${object.x_studio_many2one_field_8mpil.work_email}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
        Hello ${object.x_studio_many2one_field_8mpil.name}
        <br/><br/>
        Your
        % set access_url=object.get_order_url()
        ${doc_name} <a  href="${access_url}">${object.name}</a>
        % if object.origin:
            (with reference: ${object.origin} )
        % endif
        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}</strong> is confirmed.
        <br/><br/>
        Do not hesitate to contact us if you have any questions.
        <br/>
    </p>
</div>
            </field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="False"/>
        </record>

        <record id="email_template_agent_task_status_change" model="mail.template">
            <field name="name">Agent project task status change email template</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="subject">Re : ${object.name}</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="email_to">${object.sale_line_id.order_id.x_studio_many2one_field_8mpil.work_email}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        Hello ${object.sale_line_id.order_id.x_studio_many2one_field_8mpil.name}
        <br/><br/>
        Your task
        % set access_url=object.get_order_url()
        <a  href="${access_url}">${object.name}</a> has been updated.
        <br/>
    </p>
</div>
            </field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="False"/>
        </record>


        <record id="email_template_agent_manager_order_confirmation" model="mail.template">
            <field name="name">Agent manager sale order confirmation email</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="subject">${object.company_id.name} : ${object.x_studio_many2one_field_8mpil.name} order ${object.name or 'n/a' } has been confirmed.</field>
            <field name="email_from">${(object.user_id.email_formatted or user.email_formatted) | safe}</field>
            <field name="email_to">${object.x_studio_many2one_field_8mpil.parent_id.work_email}</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
        Hello ${object.x_studio_many2one_field_8mpil.parent_id.name}
        <br/><br/>
        Agent ${object.x_studio_many2one_field_8mpil.name} Order No.
        % set access_url=object.get_order_url()
        ${doc_name} <a  href="${access_url}">${object.name}</a>
        % if object.origin:
            (with reference: ${object.origin} )
        % endif
        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}</strong> is confirmed.
        <br/><br/>
        Do not hesitate to contact us if you have any questions.
        <br/>
    </p>
</div>
            </field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="False"/>
        </record>

    </data>
</odoo>