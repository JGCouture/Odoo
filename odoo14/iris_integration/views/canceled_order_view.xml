<?xml version="1.0" encoding="utf-8" ?>
<odoo>

	<record id="rotation_canceled_user_tree_id" model="ir.ui.view">
			<field name="name">rotation.canceled.user.tree</field>
			<field name="model">rotation.canceled.user</field>
			<field name="arch" type="xml">

				<tree string="Rotation Canceled User ">
					<field name="user_id" />
					<field name="sale_order"/>

				</tree>
			</field>
	</record>

    <record id="action_rotation_canceled_user" model="ir.actions.act_window">
	        <field name="name">Rotation Canceled User</field>
	        <field name="res_model">rotation.canceled.user</field>
	        <!-- <field name="view_type">form</field> -->
	        <field name="view_mode">tree,form</field>
	    </record>

    <menuitem id="rotation_canceled_user" name="Rotation Canceled User" parent="sale.menu_sale_config" action="action_rotation_canceled_user" sequence="21"/>
</odoo>